const constants = {
    STATUS: {
        PROPOSED: 'proposed',
        ACCEPTED: 'accepted',
        DENIED: 'denied',
        EXPIRED: 'expired',
        CANCELLED: 'cancelled',
        NOT_INVITED: 'not-invited'
    }
}

module.exports = Object.freeze(constants);